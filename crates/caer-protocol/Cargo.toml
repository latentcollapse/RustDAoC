[package]
name = "caer-protocol"
description = "DoL-lineage DAoC wire protocol: framing, checksum, codecs, crypto handshake. Headless, no rendering deps."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
thiserror = "1"

[dev-dependencies]
# Golden-trace tests compare against captured original-client sessions (added under tests/).
