[package]
name = "caer-script"
description = "mlua addon host: Lua 5.1 sandbox, typed events/commands, fault isolation, hot reload."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
mlua = { version = "0.11", default-features = false, features = ["lua51", "vendored"] }
